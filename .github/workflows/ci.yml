name: ci
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: python -m venv .venv && .venv/bin/pip install -q -e . pre-commit build
      - run: make test
      - name: wheel contains rules but not vendor test sources
        run: |
          .venv/bin/python -m build -q --wheel
          .venv/bin/python - <<'PY'
          import glob, zipfile
          names = zipfile.ZipFile(glob.glob("dist/*.whl")[0]).namelist()
          assert any(n.endswith("rules/vendor/php/lang/security/weak-crypto.yaml") for n in names), "vendor yaml missing"
          assert any(n.endswith("rules/custom/go/constant-time-compare.yaml") for n in names), "custom yaml missing"
          assert not any(n.endswith((".go", ".php", ".js")) for n in names), "test sources leaked into wheel"
          assert any(n.endswith("asvs/asvs-4.0.3.flat.json") for n in names)
          PY
  dogfood-report-only:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: ./
        with:
          paths: fixtures
          secrets: none
          upload-sarif: "false"
      - run: grep -q "V6.2.8" semgrep-asvs-out/coverage.md
  dogfood-gates-block:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - id: gates
        uses: ./
        continue-on-error: true
        with:
          paths: fixtures
          strict: "true"
          secrets: dir
          upload-sarif: "false"
      - run: test "${{ steps.gates.outcome }}" = "failure"
      - run: grep -q "V2.10.4" semgrep-asvs-out/coverage.md
  self-scan:
    # This repo's own history must be clean: .gitleaks.toml allowlists only fixtures/secrets/.
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - run: bash semgrep_asvs/install-gitleaks.sh "$HOME/.local/bin" && "$HOME/.local/bin/gitleaks" git --no-banner --redact -c .gitleaks.toml .
